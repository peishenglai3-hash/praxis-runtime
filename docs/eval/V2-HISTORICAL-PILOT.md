# V2-A Real Historical Slice Pilot

**Status:** WAITING — no real historical run is claimed.

The allowlisted Honghu archive is locally readable for bounded discovery, but
its licence/consent status is `UNVERIFIED`, and the current author instruction
does not authorize copying raw or quoted dialogue into a repository fixture.
The bounded GF01 search found locator records but not the complete required
chain. See `GF01-SOURCE-MAP.md`.

The correct next action is a local evaluator adapter over explicitly approved
record IDs, with an input window and a future holdout. No full archive ingest,
synthetic GF01 substitution, or post-hoc relabelling is permitted.

Current evidence: holdout enforcement is tested; real historical task
outcomes, human accept/reject comparisons, and Asset benefit are **unknown**.
