# Empirical compatibility probe — raw working material

**Project:** `praxis-runtime` at `C:\Users\35636\Documents\ChatGPT\伟大 光荣 正确的开源思想万岁`
**Probe date:** 2026-09-18
**Host:** Windows 11 Home China 10.0.26200, git-bash (MSYS2) shell

Status vocabulary, drawn from this set and no other: `TESTED-PASS`, `TESTED-FAIL`, `PARTIALLY TESTED`, `NOT TESTED`, `UNKNOWN`. Not every value in the set is used; `PARTIALLY TESTED` and `UNKNOWN` appear nowhere in the tables below, because no probe landed in either category.

Scratch work used `mktemp -d`; both temp trees were removed at the end (`ls -d` returns "No such file or directory" for both). No file under `packages/`, `apps/`, `migrations/`, `schemas/`, `tests/`, or `evals/` was modified. No git state was changed; only read-only git commands were run. No push, commit, or tag was performed.

---

## 1. Non-ASCII and space paths

Scratch root: `BASE=/tmp/tmp.gv2iuLyJb0` → `C:\Users\35636\AppData\Local\Temp\tmp.gv2iuLyJb0` (`cygpath -w`).

| probe                                                                                                    | command                                                                               | observed                                                                                                                                                            | status      |
| -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| Create temp dir with Chinese chars **and** a space                                                       | `mkdir -p "$BASE/测试 目录 with space"`                                               | created; `ls` lists it on the next call                                                                                                                             | TESTED-PASS |
| Create file with mixed Chinese/ASCII name inside it                                                      | `printf 'hello 世界 payload\n' > "$BASE/测试 目录 with space/混排 file 名.txt"`       | `-rw-r--r-- 1 35636 197610 21 ... 混排 file 名.txt`                                                                                                                 | TESTED-PASS |
| Read back with `node:fs` (script)                                                                        | `node probe1.mjs`                                                                     | `"readBack": "hello \u4e16\u754c payload\n"`, `"readBackCodePoints": [104,101,108,108,111,32,19990,30028,32,112,...]`, `"matchesExpected": true`                    | TESTED-PASS |
| Write with `node:fs` to a new mixed name (`node written 文件 ✦.txt`, incl. non-CJK U+2726) and read back | `node probe1.mjs`                                                                     | `"ownWritten": true`, `"ownRoundTrip": true`, `"entriesAfter"` lists both files                                                                                     | TESTED-PASS |
| Read back with `node -e`, path written with **backslashes** in the inline string                         | `node -e "...readFileSync('C:\\Users\\...\\测试 目录 with space\\混排 file 名.txt')"` | `READ_FAIL ENOENT: no such file or directory, open 'C:\Users\35636\AppData\Local\Temp\Usersî36AppDataLocalTemp\tmp.gv2iuLyJb0测试 目录 with space混排 file 名.txt'` | TESTED-FAIL |
| Read back with `node -e`, **forward slashes** (removes the escaping layer)                               | `node -e "...readFileSync('C:/Users/.../测试 目录 with space/混排 file 名.txt')"`     | `READ_OK len=17 cps=[104,101,108,108,111,32,19990,30028,32,112,97,121,108,111,97,100,10]`                                                                           | TESTED-PASS |
| Read back with `node -e`, path passed as **argv**                                                        | `node -e "...readFileSync(process.argv[1])" "C:/Users/.../混排 file 名.txt"`          | `READ_OK len=17`                                                                                                                                                    | TESTED-PASS |
| `node:sqlite` `DatabaseSync` created inside the CJK+space dir, one row written                           | `node probe1-sqlite.mjs`                                                              | `"opened": true`, `"rows": [{"id":1,"name":"\u503c value \u884c"}]`, `"closed": true`, `"dbExists": true`, `"dbSize": 8192`                                         | TESTED-PASS |
| Repository's own path (spaces + Chinese) gated successfully                                              | `cat test-results/gate-result.json`                                                   | file exists; `"result": "PASS"`, `"complete": true`, all 12 stages `"status": "PASS"`, `"notRun": []`                                                               | TESTED-PASS |

**Interpretation of the one `TESTED-FAIL`.** The failure is in the _escaping layer_, not in Node's path handling. In the failing invocation the JS source reached Node with single backslashes, so JS itself consumed them (`\t` → TAB, `\U` → `U`, `\3` → `3`, and so on). The same path, freed of that layer, reads correctly both inline and via argv. The CJK path itself round-trips.

**Caveat attached to the gate-result.json row.** The artifact is real and says `PASS`, but its own `runtime` block records `"node": "24.15.0"`, `"pinned": "22.13.0"`, `"matchesPin": false`, `"dirty": true`, and `"commit": "67c22754ef13dc9fc41e43c6ccc4df745d5d2084"` — which is **not** the current `HEAD` (`8c459e9993b64ca715d10dda2df2cd8542b6dff8`). See sections 6 and 7.

---

## 2. Long paths

| probe                                                                            | command                                                                             | observed                                                                                                                                 | status      |
| -------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| Build a nested path beyond 260 chars and create a file at the end with `node:fs` | `node probe2.mjs`                                                                   | 14 nested dirs of 23 chars each; **every** depth succeeded. Deepest: `"dirLen": 393`, `"leafLen": 402`, `"mkdir": "ok"`, `"write": "ok"` | TESTED-PASS |
| Regrouped: shortest tested depth over 260                                        | same                                                                                | depth 9 → `leafLen: 282`, `"write": "ok"`; depth 8 → `leafLen: 258`, `"write": "ok"`                                                     | TESTED-PASS |
| `\\?\` extended-length prefix on the 402-char leaf                               | `node probe2.mjs`                                                                   | `"extendedWrite": "ok"`, `"extendedLeafExists": true`                                                                                    | TESTED-PASS |
| Windows long-path policy state at probe time                                     | `reg query "HKLM\SYSTEM\CurrentControlSet\Control\FileSystem" //v LongPathsEnabled` | `LongPathsEnabled    REG_DWORD    0x0`                                                                                                   | TESTED-PASS |
| `core.longpaths` git setting                                                     | `git config --get core.longpaths`                                                   | unset (no output)                                                                                                                        | TESTED-PASS |

A 402-character path was created and written with plain `node:fs` on this host while `LongPathsEnabled` read `0x0`. That is the observation. The mechanism by which it succeeds was **not** established by this probe — no test was run that isolates Node's path handling from the OS's.

Longest path actually exercised: 402 characters. Behaviour beyond that: `NOT TESTED`.

---

## 3. Read-only directory

| probe                                                                                             | command                                                                      | observed                                                                                                                                        | status      |
| ------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| `chmod -w` on a directory blocks writes                                                           | `chmod -w "$BASE/rotest"` then `echo probe > "$BASE/rotest/after-chmod.txt"` | `chmod exit=0`, but the write succeeded: `WRITE STILL SUCCEEDED (chmod -w ineffective)`; file present in `ls`                                   | TESTED-FAIL |
| `icacls` deny-write ACE applies                                                                   | `MSYS_NO_PATHCONV=1 icacls "<winpath>" /deny "35636:(W)"`                    | `已处理的文件: C:\...\rotest2` / `已成功处理 1 个文件; 处理 0 个文件时失败`; `icacls` read-back shows `LPS\35636:(DENY)(W)`                     | TESTED-PASS |
| Plain `node:fs` write under the deny ACE                                                          | `node probe3.mjs "<winpath>"`                                                | `{"name":"Error","code":"EPERM","errno":-4048,"syscall":"open","message":"EPERM: operation not permitted, open 'C:\\...\\rotest2\\plain.txt'"}` | TESTED-PASS |
| `node:sqlite` `DatabaseSync` opening a **new** DB in the denied dir                               | `node probe3.mjs "<winpath>"`                                                | `{"name":"Error","code":"ERR_SQLITE_ERROR","message":"unable to open database file"}`                                                           | TESTED-PASS |
| `node:sqlite` `DatabaseSync` opening a **pre-existing, file-level-writable** DB in the denied dir | same (rotest3, `pre.db` created before the ACE)                              | identical class: `{"name":"Error","code":"ERR_SQLITE_ERROR","message":"unable to open database file"}`                                          | TESTED-PASS |

**Error classes asked for:** `node:fs` raises `EPERM` (errno `-4048`, syscall `open`). `node:sqlite` raises `ERR_SQLITE_ERROR` with the message `unable to open database file` — it does **not** surface the underlying `errno`/syscall, and the class is the same whether or not the DB file itself already exists (the directory-level denial is enough to stop it).

First `icacls` attempt failed spuriously: without `MSYS_NO_PATHCONV=1`, git-bash rewrote the `/deny` flag to `C:/Program Files/Git/deny` → `无效参数`. Recorded because it is a real hazard for any documentation that tells a user to run `icacls` from git-bash.

Cleanup: `/remove:d` was applied to both dirs; `icacls` read-back shows the DENY entry gone, and a subsequent write succeeded (`WRITE RESTORED`).

---

## 4. Shell sensitivity of the build

| probe                                                                                                                           | command                                                    | observed                                                                                                                                                                                                                                                              | status      |
| ------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| `gate.mjs` spawns stages without a shell                                                                                        | Read `scripts/gate.mjs`                                    | `runStage` at lines 148–152: `const result = spawnSync(invocation[0], [...invocation.slice(1), "run", stage.id], { cwd, shell: false, stdio: ["ignore", "inherit", "inherit"] },);`                                                                                   | TESTED-PASS |
| `gate.mjs` also spawns `git` without a shell                                                                                    | Read `scripts/gate.mjs`                                    | lines 131–135: `const result = spawnSync("git", args, { cwd, encoding: "utf8", shell: false, });`                                                                                                                                                                     | TESTED-PASS |
| Documented rationale matches the code                                                                                           | Read `scripts/gate.mjs` header                             | "**No shell, ever.** Every stage is spawned with `shell: false` and an argv array, so no pipeline can exist to contribute an exit status. There is no `\| tail` anywhere in this file…"                                                                               | TESTED-PASS |
| Scan all 35 `package.json` scripts for `\|`, `$(`, backticks, single-quote quoting, `;`, `*`, `>`, `<`, `^`, `%`, `~`, `\`, `&` | `node -e` over `pkg.scripts`                               | **One** metacharacter found, `&`/`&&`, in **6** scripts: `typecheck`, `verify:lint`, `verify:typecheck`, `smoke:cli`, `smoke:daemon`, `verify:chain`. No pipeline, no command substitution, no backticks, no single-quote quoting, no redirection anywhere.           | TESTED-PASS |
| Scripts using `&&` (the full list)                                                                                              | same                                                       | `typecheck` → `tsc --build … && tsc -p …`; `verify:lint` → 5 × `pnpm … && …`; `verify:typecheck` → 3 × `pnpm … && …`; `smoke:cli` → 8 × `pnpm … && …`; `smoke:daemon` → `pnpm phase35:scenario && node scripts/daemon-smoke.mjs`; `verify:chain` → 12 × `pnpm … && …` | TESTED-PASS |
| Shell used to run those scripts                                                                                                 | `npm config get script-shell`                              | `null` (unset → pnpm platform default)                                                                                                                                                                                                                                | TESTED-PASS |
| `.gitattributes` normalises line endings                                                                                        | `cat .gitattributes`                                       | `* text=auto eol=lf` — yes, normalised to LF repo-wide, with two deliberate opt-outs: `fixtures/legacy/** -text` and `tests/replay/*.ndjson -text` (both documented as byte-exact golden inputs)                                                                      | TESTED-PASS |
| Stage script is location-independent                                                                                            | `cd /tmp && node "C:/…/scripts/assert-cycle-detected.mjs"` | `Error: Cannot find module 'C:\Users\35636\AppData\Local\Temp\node_modules\dependency-cruiser\bin\dependency-cruise.mjs'`, exit 1                                                                                                                                     | TESTED-FAIL |

**On `&&`.** It is the only shell construct in the scripts. It is accepted by `cmd.exe` and by PowerShell 7+. It is **not** accepted by PowerShell 5.1 (parse error). Whether it breaks under a PowerShell host was **not** tested — `&&` was never executed under PowerShell during this probe. No script in `package.json` was actually run through PowerShell or `cmd.exe`; the finding above is a syntactic scan of the script strings, plus `gate.mjs` read as source.

**On the `TESTED-FAIL` row.** `scripts/assert-cycle-detected.mjs` resolves its tool with `resolve("node_modules/dependency-cruiser/bin/dependency-cruise.mjs")` — i.e. against `process.cwd()`, not against the script's own directory (`assert-boundaries-detected.mjs` does it correctly, using `rootDir`; the two differ). The gate is unaffected because `runStage` passes `cwd: root`. The script is not runnable from any other directory.

---

## 5. Network independence of the gate

Stage list, read from `scripts/gate.mjs` (`stages`) and cross-checked programmatically against `package.json#scripts["verify:chain"]`:

```
gate stage list (12): verify:runtime, verify:deps, verify:lint, verify:typecheck,
  test:unit, test:integration, test:replay, test:regression, test:evals, build,
  smoke:cli, smoke:daemon
parsed verify:chain (12): (identical, in order)
AGREE: true
validate:6v0 in chain? false
any chain stage referencing tests/validation? false
```

| probe                                                                                                                                                                                                                           | command                                                                                                     | observed                                                                                                                                                                                                                                                                                                                               | status      |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| Repo-wide search for network APIs: `node:http`, `node:https`, `node:net`, `node:dns`, `require('http')`, `from 'https'`, `undici`, `axios`, `node-fetch`, `fetch(`, `WebSocket`, `.listen(`, `createServer`, `createConnection` | `Grep` over `*.{mjs,js,ts,cjs,json}`, `node_modules` excluded                                               | **Exactly one hit in the whole repository**: `tests/validation/validation-provider-adapter.ts:182: response = await this.#fetch(this.#endpoint, {`                                                                                                                                                                                     | TESTED-PASS |
| Is that one network caller inside a mandatory stage?                                                                                                                                                                            | `node -e` importing `scripts/gate.mjs`, comparing `verifyChain(pkg.scripts["verify:chain"])` to `stages`    | `validate:6v0 in chain? false`; the only script pointing at `tests/validation` is `validate:6v0`, which is not a gate stage                                                                                                                                                                                                            | TESTED-PASS |
| Does that caller's own test self-gate?                                                                                                                                                                                          | Read `tests/validation/phase6v0.test.ts`                                                                    | `const describeWithKey = apiKey.length === 0 ? describe.skip : describe;` and the header comment: "It needs a live credential, so it must never run inside a gate. It is skipped unless `PRAXIS_VALIDATION_PROVIDER_KEY` is set, and no suite script points at `tests/validation`, so `pnpm verify` cannot reach it even by accident." | TESTED-PASS |
| Is the key set on this host?                                                                                                                                                                                                    | `if [ -n "${PRAXIS_VALIDATION_PROVIDER_KEY:-}" ]`                                                           | `NOT SET`                                                                                                                                                                                                                                                                                                                              | TESTED-PASS |
| Second candidate endpoint (`https://example.invalid/v1`)                                                                                                                                                                        | Read `packages/adapters/src/conformance.ts:570–600`                                                         | It is a **string field inside an observation record** being asserted for credential redaction (`providerMetadata: { endpoint: "https://example.invalid/v1", … }`), next to `assert.equal(serialised.includes(credentialShaped("sk-")), false, …)`. It is never dialled. (`example.invalid` is also a reserved non-resolving TLD.)      | TESTED-PASS |
| Third candidate (`$ref: "https://example.invalid/other.json"`)                                                                                                                                                                  | Read `evals/test/schema.test.ts:187`                                                                        | A `$ref` value in a schema-deserialisation test; `validate:6v0` is not the only user — this is `test:evals`, stage 9. It is a schema string, not a fetch. No HTTP client is imported by `evals/test`.                                                                                                                                  | TESTED-PASS |
| `test:evals` harness deliberately offline                                                                                                                                                                                       | Read `evals/src/runner.ts:44`                                                                               | "…is what lets the oracle self-test drive the whole pipeline with **no model and no network**." Stage 9's own label in `gate.mjs` is `"evaluation harness self-tests (no model, no network)"`.                                                                                                                                         | TESTED-PASS |
| Data-driven command execution in evals                                                                                                                                                                                          | `Grep "oracleCommands"` repo-wide                                                                           | Only ever referenced in `evals/src/scenario.ts` and `evals/src/run.ts` (the machinery) plus one doc table. **No scenario or corpus file in the repo populates `oracleCommands`**, so no external command is executed by the eval path today. `run.ts:86` would spawn it with `shell: false`.                                           | TESTED-PASS |
| `smoke:daemon` transport                                                                                                                                                                                                        | Read `scripts/daemon-smoke.mjs`; `Grep` for `listen\|socket\|pipe\|spawn` in `apps/daemon/src` + the script | Spawns `spawnSync(process.execPath, [entry, ...args], { cwd, timeout: 60_000, env: {…} })` — a one-shot child Node process, no listener. No `listen`/`socket` anywhere in `apps/daemon/src`.                                                                                                                                           | TESTED-PASS |
| `build` stage                                                                                                                                                                                                                   | Read all 12 workspace `package.json` files                                                                  | Every package builds with local `tsup …` (e.g. `"build": "tsup src/index.ts --tsconfig ../../tsconfig.json --format esm --dts --out-dir dist"`). No package registry access.                                                                                                                                                           | TESTED-PASS |
| Fixture assertion scripts                                                                                                                                                                                                       | Read `scripts/assert-cycle-detected.mjs`, `scripts/assert-boundaries-detected.mjs`                          | Both spawn the local `node_modules/dependency-cruiser/bin/dependency-cruise.mjs` under `process.execPath`. No network.                                                                                                                                                                                                                 | TESTED-PASS |
| Does the gate install dependencies?                                                                                                                                                                                             | Read `scripts/assert-runtime-pins.mjs`                                                                      | No. It only checks that `pnpm-lock.yaml` and `pnpm-workspace.yaml` **exist** (`if (!existsSync(…)) fail(…)`). Its own header says: "This is not a substitute for CI's `pnpm install --frozen-lockfile`". No `pnpm install` appears in `verify:chain`.                                                                                  | TESTED-PASS |

**Answer, on the evidence above: no mandatory stage of the gate requires network access.** Every one of the twelve stages was traced to its entry point, and the only HTTP client in the repository is behind a stage that is not in the chain, is additionally key-gated, and is additionally skipped on this host for want of the key.

**What would strengthen or falsify this, and was not run.** The gate was not executed with the network physically disabled, and no stage was executed under a firewall/sandbox. The claim above is therefore a source-level claim about what the code does, not a runtime observation of the gate surviving disconnection. A live no-network execution of the full chain: `NOT TESTED` — it would take roughly the 124 s recorded in `gate-result.json`, plus setup, and was outside the remaining budget for this probe.

---

## 6. Runtime and architecture facts

| probe                                                    | command                                                                  | observed                                                                                                               | status      |
| -------------------------------------------------------- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------- | ----------- |
| Running runtime                                          | `node -e "console.log(process.platform, process.arch, process.version)"` | `win32 x64 v24.15.0`                                                                                                   | TESTED-PASS |
| Interpreter in use                                       | `which node`                                                             | `/c/Program Files/nodejs/node`                                                                                         | TESTED-PASS |
| `.node-version`                                          | `cat .node-version`                                                      | `22.13.0`                                                                                                              | TESTED-PASS |
| `.nvmrc`                                                 | `cat .nvmrc`                                                             | `22.13.0`                                                                                                              | TESTED-PASS |
| `package.json` `engines`                                 | read                                                                     | `"node": ">=22.13.0 <23"`, `"pnpm": "11.19.0"`; `"packageManager": "pnpm@11.19.0"`                                     | TESTED-PASS |
| CI pin (what `assert-runtime-pins.mjs` compares against) | `grep node-version .github/workflows/*.yml`                              | `.github/workflows/ci.yml:28: node-version: 22.13.0`; `.github/workflows/provider-smoke.yml:21: node-version: 22.13.0` | TESTED-PASS |
| Do all four agree?                                       | `node scripts/assert-runtime-pins.mjs`                                   | `runtime pins PASS (node 24.15.0, pnpm@11.19.0, pins agree, lockfile present)`, exit 0                                 | TESTED-PASS |
| Installed pnpm                                           | `pnpm --version`                                                         | `11.19.0` — matches `packageManager`                                                                                   | TESTED-PASS |
| `node:sqlite` on the running Node                        | `node -e "require('node:sqlite')"`                                       | loads with no flag: `sqlite module loaded, exports: DatabaseSync,StatementSync,Session,constants,backup`               | TESTED-PASS |

**Agreement.** `.node-version` (22.13.0), `.nvmrc` (22.13.0), `.github/workflows/ci.yml` (22.13.0), and the `assert-runtime-pins.mjs` check all agree on the exact version `22.13.0`. `package.json#engines.node` describes the admissible range as `>=22.13.0 <23`, whose floor is that same version — so the range is consistent with the pins rather than agreeing on a literal string. `assert-runtime-pins.mjs` enforces exactly this: `.node-version` must equal `.nvmrc`, both must lie inside `engines.node`, and `ci.yml`'s `node-version` must equal `.node-version`.

**The running runtime is not the pinned runtime.** `process.version` is `24.15.0`; the pin is `22.13.0`; `engines.node` is `>=22.13.0 <23`. `24.15.0` satisfies the floor but not the ceiling. `gate-result.json` records the same disagreement as data: `"node": "24.15.0"`, `"pinned": "22.13.0"`, `"matchesPin": false`.

**The out-of-range warning does not fire.** `assert-runtime-pins.mjs` contains a warning for exactly this case (`"running Node ${current}, outside the pinned range ${range}; this run is development evidence, not the pinned-runtime evidence CI provides"`), but the run above printed **no** `[warn]` line. The reason is visible in the same file: the bound is extracted by `const maximum = /<\s*([0-9]+\.[0-9]+\.[0-9]+)/.exec(range)?.[1];`, which requires three dotted components, while the declared range is `>=22.13.0 <23`. `maximum` is therefore `undefined` and the warning branch is unreachable for this `engines` string. Both facts are read directly from the source; the absence of the warning is the observed run above.

**Other Node versions installed.** None. `command -v` finds no `nvm`, `fnm`, `volta`, or `nvs`; directory checks for `~/AppData/Roaming/nvm`, `~/.volta`, `~/AppData/Roaming/fnm`, and `C:\nvm` all return "No such file or directory". The only Node installation is `C:\Program Files\nodejs` (v24.15.0). **Node 22.13.0 is not installed on this host, so no behaviour under the pinned runtime could be tested.**

**Related in-repo documentation** (a claim recorded by the project, not a measurement made by this probe — see `## Facts I could not establish`): `docs/断点记录.md:231` states "Node v22.12.0 文档仍要求 `--experimental-sqlite`，v22.13.0 起才取消该 flag 要求但仍标注 experimental", and `docs/断点记录.md:232` states "本机当前验证版本为 Node `v24.15.0`，不能证明 `v22.13.0` CI 行为". The same item appears as BP-010 in `docs/engineering/_INVENTORY-RAW.md:46`, whose status string is `已修正并保留待验证` with a `### 待验证` section noting that the remote 22.13.0 runner's frozen install, typecheck, tests, build, concurrency, and crash runs remain pending.

---

## 7. Repository state

| probe                                             | command                                                                    | observed                                                                                                                                                                                                        | status      |
| ------------------------------------------------- | -------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| Branch                                            | `git rev-parse --abbrev-ref HEAD`                                          | `docs/phase6v-prep`                                                                                                                                                                                             | TESTED-PASS |
| HEAD commit                                       | `git rev-parse HEAD`                                                       | `8c459e9993b64ca715d10dda2df2cd8542b6dff8`                                                                                                                                                                      | TESTED-PASS |
| Working tree, **first** reading (~12:58 local)    | `git status --porcelain`                                                   | one line: `?? docs/ADR/ADR-0015-evaluation-harness-boundary.md` (untracked; no tracked file modified)                                                                                                           | TESTED-PASS |
| Working tree, **second** reading (13:02:53 local) | `git status --porcelain`                                                   | **twelve** entries, up from one — see the drift note below                                                                                                                                                      | TESTED-PASS |
| Tracked files modified, either reading            | `git status --porcelain \| grep -v '^??'`                                  | nothing. Every entry in both readings is `??` (untracked). No tracked file was modified.                                                                                                                        | TESTED-PASS |
| Commits ahead of `origin/main`                    | `git rev-list --count origin/main..HEAD`                                   | `39`                                                                                                                                                                                                            | TESTED-PASS |
| Remote reachable                                  | `timeout 30 git ls-remote --exit-code origin HEAD`                         | `15f6e2f9e32f5b5a803330cab93a175a5d1d52f6	HEAD`, `exit=0` — succeeded on the **first** attempt, well inside the 30 s timeout, so no second attempt was made                                                      | TESTED-PASS |
| Remote identity                                   | `git remote -v`                                                            | `origin https://github.com/peishenglai3-hash/praxis-runtime.git` (fetch and push). No credentials embedded in the URL; nothing was printed that would need redaction.                                           | TESTED-PASS |
| Gate artifact is current                          | compare `test-results/gate-result.json` `commit` with `git rev-parse HEAD` | artifact `commit: 67c22754ef13dc9fc41e43c6ccc4df745d5d2084` vs HEAD `8c459e9993b64ca715d10dda2df2cd8542b6dff8` — **the artifact certifies an earlier commit than the one checked out.** Artifact `dirty: true`. | TESTED-PASS |
| Artifact timestamp                                | read                                                                       | `startedAt 2026-09-18T04:54:54.880Z`, `endedAt 2026-09-18T04:56:58.754Z`, `durationMs 123874` (~124 s for all 12 stages)                                                                                        | TESTED-PASS |
| Harness's own claim about the working directory   | environment preamble                                                       | the run was launched with `Is a git repository: false` for this directory, yet `git rev-parse --abbrev-ref HEAD` returns `docs/phase6v-prep` from it. The preamble is wrong about this directory.               | TESTED-PASS |

**The working tree moved while this probe was running.** This is the most important caveat in this section, and it invalidates any reading of section 7 as a stable snapshot.

The two `git status --porcelain` readings above were taken about four minutes apart and disagree: one untracked entry, then twelve. `HEAD` did **not** change between them (`8c459e9993b64ca715d10dda2df2cd8542b6dff8` both times), and no tracked file was modified in either reading. The evidence that this was an external writer rather than a stale index is the modification times of the entries that appeared:

```
2026-09-18 13:02:41.081  docs/ADR/ADR-0015-evaluation-harness-boundary.md
2026-09-18 13:02:41.132  docs/engineering/FAILURE-FAMILIES.md
2026-09-18 13:02:41.201  docs/engineering/ARCHITECTURE-SELF-REVIEW.md
2026-09-18 13:02:41.304  docs/eval/VERIFIER-RELIABILITY-REPORT.md
2026-09-18 13:02:41.338  docs/eval/SCENARIO-COVERAGE-MATRIX.md
2026-09-18 13:02:41.404  docs/eval/PHASE-6V-EMPIRICAL-REPORT.md
2026-09-18 13:02:41.473  tests/regression/fault-injection.test.ts
2026-09-18 13:02:36.877  docs/eval/_COMPAT-PROBE-RAW.md        <- written by this probe
2026-09-18 13:02:52.680  docs/engineering/RFC-MISMATCH-RECONCILIATION.md
```

Everything except this probe's own file lands in a single ~400 ms batch at `13:02:41`, with one further write eleven seconds later. `tests/regression/fault-injection.test.ts` is in that batch: it was already present and readable earlier in the probe (it was listed by `ls tests/regression/` and searched by `Grep`, which found content at line 32), so it was not created by this probe — it was rewritten by the external writer, and the rewrite is what made it appear as untracked.

Consequences that a formal report should carry:

1. **`test-results/gate-result.json` was produced under the same conditions.** It records `"dirty": true`, and its `commit` (`67c22754ef13…`) is already two commits away from `HEAD` (`8c459e99…`). The gate certified a worktree that was both dirty and different from what is checked out now.
2. **Any "the gate passes" statement is a statement about a worktree that no longer exists.** Re-running the gate would certify a different tree.
3. **Section 1's repo-path row should be read with this in mind.** The `PASS` in `gate-result.json` is genuine and the path does contain spaces and Chinese, but the run it describes is not reproducible from the current checkout.

No git state was modified by this probe: no commit, no tag, no push, no checkout, no reset, no `git add`, no index write. The untracked files listed above were not created or edited by this probe, with the single exception of `docs/eval/_COMPAT-PROBE-RAW.md`, which is this report.

---

## Probes I could not run

| probe                                                                                                                                                                   | why                                                                                                                                                                                                                                                                                                                                                                           |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Any behaviour under the **pinned runtime, Node 22.13.0** — including whether `node:sqlite` needs `--experimental-sqlite` there, and whether the whole gate passes there | Node 22.13.0 is not installed on this host. The only interpreter is `C:\Program Files\nodejs` v24.15.0; `nvm`, `fnm`, `volta` and `nvs` are all absent (both `command -v` and directory checks). Installing a runtime was out of scope ("Do not install anything"). This is the single largest gap in this probe: every runtime-behaviour row above is Node 24.15.0 evidence. |
| Executing the **full gate with the network physically disabled**                                                                                                        | Not attempted: it would require the ~124 s chain plus setup and a sandbox/firewall arrangement, and the budget for this probe was ~45 minutes with the chain already recorded as passing. The section 5 answer therefore rests on source inspection of all twelve stage entry points, not on a disconnected run.                                                              |
| Running any `package.json` script **under PowerShell or `cmd.exe`** to see whether `&&` actually works there                                                            | Not attempted; this was a syntactic scan plus a source read of `gate.mjs`. `script-shell` is unset (`null`), so the default host shell was never switched. The `&&`-under-PowerShell-5.1 hazard is stated from the operator's general knowledge of `&&`, **not** from a measurement taken here.                                                                               |
| Long-path behaviour **beyond 402 characters**                                                                                                                           | The deepest path built was 402 chars, and that succeeded at every depth short of it, so there was no error boundary to record. The actual ceiling on this host remains unmeasured.                                                                                                                                                                                            |
| A stage-by-stage **timing/profile** of the gate                                                                                                                         | Not requested, and `gate-result.json` already carries per-stage `durationMs`, which was read instead.                                                                                                                                                                                                                                                                         |
| Whether `node:sqlite`'s `ExperimentalWarning` interferes with the daemon's stderr parsing in practice                                                                   | Read from source (`scripts/daemon-smoke.mjs` explicitly handles it: it takes "the last line of `text` that is a JSON object" because "on a refusal it writes it to **stderr** — the same stream Node uses for `ExperimentalWarning`"). Not executed as a separate probe.                                                                                                      |
| Any behaviour of `validate:6v0` / `tests/validation` against a live provider                                                                                            | Requires `PRAXIS_VALIDATION_PROVIDER_KEY`, which is `NOT SET` on this host, and the test self-skips. No credential was sought or supplied.                                                                                                                                                                                                                                    |

## Facts I could not establish

- **Whether the gate passes on Node 22.13.0.** The pin is 22.13.0 everywhere; the only gate run on record (`test-results/gate-result.json`) ran on 24.15.0 with `matchesPin: false`. No run on the pinned runtime was observed by this probe, and none exists in the artifact.
- **Whether `node:sqlite` requires `--experimental-sqlite` on 22.13.0.** Unflagged loading was confirmed only on 24.15.0. The repository's own `docs/断点记录.md:231` asserts the flag requirement ends at v22.13.0, but that is a documentation claim read from the repo, not a measurement made here, and the same document (`:232`) states the project itself has not proven 22.13.0 CI behaviour locally.
- **The mechanism by which 402-character paths succeed while `LongPathsEnabled` reads `0x0`.** The success is observed; whether it comes from Node's own path handling, from the process manifest, or from some other cause was not isolated.
- **Whether `&&` in the six scripts is safe under PowerShell.** Untested. It is safe under `cmd.exe` and PowerShell 7+ on general knowledge, but nothing was executed under either shell.
- **Whether `git ls-remote` succeeding implies the gate would succeed from a clean clone.** The remote is reachable and holds `15f6e2f9…` at `HEAD`, which is neither the local `HEAD` (`8c459e99…`) nor the artifact's commit (`67c22754…`). The relationship between those three commits was not investigated.
- **What is writing to the working tree, and whether it has stopped.** A batch of seven files appeared at `13:02:41` local with one further write at `13:02:52` (section 7). The writer was not identified — no process listing was taken and none was in scope — so whether the tree is now stable, or will keep moving, could not be established. Every filesystem and git observation in this document should be treated as timestamped rather than current.
- **The number of commits `origin/main` is behind**, as distinct from the 39 the local branch is ahead — only `origin/main..HEAD` was counted, not the reverse.
- **What the single untracked file `docs/ADR/ADR-0015-evaluation-harness-boundary.md` contains or whether it is meant to be tracked.** It was observed in `git status --porcelain` and left untouched.
