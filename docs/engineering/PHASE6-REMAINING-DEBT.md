# Phase 6 Remaining Debt — Iteration 1 disposition

This register replaces indefinite `NEEDS-EVIDENCE` wording with an explicit
resolution or a bounded deferral.

| Item                            | Status                                               | Evidence / decision                                                                                                                                                                                                                                                                                                                                                                                                                                     | Iteration 2 boundary                                                                                                                                           |
| ------------------------------- | ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CLI_SURFACE_GAPS_VS_13`        | **RESOLVED**                                         | Projection-scoped rebuild and legacy report paths already exist; privacy purge now accepts positional `<session-id>` and retains `--session <id>` as a compatibility alias. Operator regression covers the positional path and conflicting forms.                                                                                                                                                                                                       | None for the current v0.x session scope. New scope kinds require a new decision.                                                                               |
| projection-lag negative control | **RESOLVED**                                         | `tests/integration/phase55-operator-surface.test.ts` injects a lagging projection and asserts doctor exit 6 plus a failing `projection:*` check.                                                                                                                                                                                                                                                                                                        | Add lower-level store fault injection only if a future projection implementation changes.                                                                      |
| CLI privacy purge semantics     | **RESOLVED**                                         | Dry-run/confirm/plan-hash behavior remains explicit; positional Bible shape is supported; `--session` is preserved for backward compatibility.                                                                                                                                                                                                                                                                                                          | Review additional scope forms only when the Bible defines them.                                                                                                |
| `DOCTOR_COVERAGE_VS_13_1`       | **DEFERRED WITH EXPLICIT PRE-RELEASE JUSTIFICATION** | Existing doctor covers writer lock, WAL/PRAGMA state, writer provenance, projection cursor/lag, orphan verification, purge state, backup integrity, asset provenance, and legacy integrity. Remaining diagnostic breadth (active-db integrity check, migration compatibility, event sampling, stale snapshot/orphan projection rows, cursor/context exposure checks) is not needed to run the empirical harness and is not silently claimed as present. | Must be resolved or explicitly accepted before formal pre-release freeze; no Release Engineering or public release may rely on full §13.1 coverage until then. |

The deferral is a scope decision, not evidence that the missing checks are
unimportant. It keeps Iteration 1 from expanding the Runtime while preserving
the blocker in the freeze-gap report.
