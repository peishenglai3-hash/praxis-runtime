* text=auto eol=lf

# The Phase 5 Legacy fixture is byte-exact on purpose. Several anomaly classes
# it must reproduce are *encoding* facts — a UTF-8 byte-order mark, a CRLF
# frontmatter anchor, a damaged JSON line — so Git must not normalise line
# endings or encoding in this directory. This is the same failure class the
# Windows runner exposed as BP-040, applied deliberately instead of by accident.
fixtures/legacy/** -text
