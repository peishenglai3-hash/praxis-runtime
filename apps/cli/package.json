{
  "name": "@praxis/cli",
  "private": true,
  "type": "module",
  "dependencies": {
    "@praxis/runtime": "workspace:*"
  },
  "scripts": {
    "build": "tsup src/index.ts --tsconfig ../../tsconfig.json --format esm --dts --out-dir dist"
  }
}
