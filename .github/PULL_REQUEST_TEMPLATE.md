<!--
Before opening this pull request:

- `pnpm verify` must return zero. Paste the result below.
- One concern per pull request. A "fix everything" commit will be asked to be
  split; see Bible section 16.
- Do not update a golden fixture to make a failing test pass. Explain the
  change instead.
- A database migration, a contract-breaking change, or a change to promotion
  semantics needs an ADR or RFC update in this same pull request.
-->

## What this changes

## Which issue or phase

<!-- ISSUE-XXX, EPIC-XXX, or the Bible section. Work belonging to a later phase
     is not accepted early, even when it is small and obviously correct. -->

## Does it diverge from the plan?

<!-- If yes, this is an RFC MISMATCH and it needs its own record before the
     code. Say so here and link it. If no, say "no". -->

## Verification

<!-- Paste `pnpm verify` output, or at least its exit status and test counts.
     State the Node and pnpm versions you ran it on. A pass on a Node version
     other than the pinned 22.13.0 is development evidence, not gate evidence. -->

```

```

## Tests

<!-- What you added or changed, and what it would catch if it regressed. -->

## Known limitations

<!-- Anything left undone, any `RFC MISMATCH` opened, any risk introduced.
     "None" is an acceptable answer only if it is true. -->
